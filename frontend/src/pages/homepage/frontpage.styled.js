import styled from 'styled-components';

export const FrontPageStyled = styled.div`

.BckImage {
    position: relative;
    top: 0;
    left: 0;
    width: 100%;
    object-fit: contain;
    z-index: -1;
  }

  .contentContainer {
    padding: 3rem;
  }

`;